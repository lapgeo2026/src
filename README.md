conda environment is necessary and Java should be installed
beforre launching: python script.py which convert *file*.pdf to *file*.csv
and search lines in which a specific pattern appears in the corresponding csv.
